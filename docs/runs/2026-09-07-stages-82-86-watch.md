# Watch log — stages 82-86, 2026-09-07

Controller: Claude Opus 5, interactive, polling at five-minute intervals.
Run started 19:33Z after the operator moved the kickoff forward from 22:00.

## Setup

Cards 82-86 queued 2026-09-06, held on `paused_until` overnight so autometta's
batch 108-134 had the machine to itself. That batch completed, which changed
two things before a single stage here dispatched:

- The contract-test gate defect this repo found (autometta cards 129 and 134)
  was fixed and re-vendored here at `0d4760db`. All five cards had criterion 2
  written against the old broken gate, naming an invocation that no longer
  exists. Re-briefed all five to `--worktree` and to report which of the three
  outcomes they got. Card 86 took a second re-brief: its Surfacing concern was
  written in the present tense about a defect that is now past, and two of its
  deliverables would have been obsolete on arrival.
- The token window rolled and this time it *did* zero `tokens_spent`. No drain
  was taken: the resting 100M cap covers a slate estimated at ~25M, and
  autometta held a live drain covering only itself. Starting one here would
  have clobbered it — the precise defect card 130 had just fixed.

## 19:33Z — stage 82 dispatched, failed at 19:39Z

Harness artefact, not a work defect. The Codex worker (GPT-5.6 Terra) could not
open a browser at all: the in-app browser reported no available instance and
the Playwright Chromium fallback was refused by macOS before reaching a page —
`bootstrap_check_in ... Permission denied (1100)`, a Mach-port rendezvous the
seat is not permitted to make.

The worker's conduct was correct and is worth recording. It ran `npm run
verify` green over 358 tests, invoked the contract guard in `--worktree` mode,
reported the no-relevant-files case in the words the re-brief asked for, then
declined to fabricate frames or write an evidence-free appendix, and stopped at
752,509 tokens.

**Remedy: re-card, not re-brief.** Nothing about the card was wrong. The worker
seat on 82 moved to `Claude Opus 5` — Claude seats drive Playwright
successfully on this machine, as stages 77, 79 and 81 did on 2026-09-06. The
same change was made pre-emptively to **85**, the other browser-capture card,
which had Terra queued to hit the identical wall.

Cost accepted: both cards now run Claude on both seats and lose the
cross-family check. What contains it is the shape of the acceptance — the
verifier looks at the frames and forms its own view before reading the prose,
which is independent judgement rather than a rerun. Fable 5.1 stays on both.

82 requeued via `requeue-stage.sh`; worktree and branch removed, fail envelope
purged, `consecutive_failures` back to 0.

## Operator changes during the run

- 01:00 stop disarmed at operator request so the run continues overnight.
- `window_reserve` armed for the 01:00 session only: top-level `percent: 0`
  with `action: hold`, and an `overnight` block of 01:00-06:00 at `percent:
  20` — percent being remaining headroom, so 20 means hold at 80% spent. The
  action was previously `observe`, which logged what it would have held and
  dispatched anyway; the gate had never actually been armed. Verified against
  the clock: 23:30 and 00:30 resolve uncapped, 01:30 and 05:30 to the 20%
  reserve, 06:30 uncapped again.

## Pairing review

The tick pairs two *adjacent* stages, gated on disjoint claims, alternating
worker families, the tail's gate, fewer than two prior pairing failures, and
p95 budget headroom. There is no N-way parallelism, so the ceiling is five
stages in three slots.

| Pair | Verdict |
|---|---|
| 82 + 83 | pairs — claude/codex, claims disjoint |
| 83 + 84 | blocked, both codex |
| 84 + 85 | blocked, both claim the Gray-Scott write-up |
| 85 + 86 | pairs — claude/codex, claims disjoint |

Moving 82 and 85 to Claude workers for the browser problem incidentally created
both opportunities, since 83, 84 and 86 are all codex. A third pair was
available by moving 84 to Claude too, and was declined: Claude already holds
all five verifier seats plus two worker seats, its 5-hour window read 81% at
19:40Z, and it is the family most likely to trip the 01:00 reserve.

## 19:48Z — stage 83 worker passed

`GPT-5.6 Sol`, 1,160,625 tokens, envelope `status: pass`. Claims the dated
structure-term bias audit with a deliberate-tradeoff verdict, structure-zero
arithmetic across nine shipped promotion decisions, and a named
phase-scrambled-spectrum test as the distinguishing experiment deliverable 3
asked for. Verifier `Claude Opus 5` dispatched 19:48:32Z.

Unverified as of this entry. The verifier's brief is to attack the central
argument — high lag-1 autocorrelation also describes a sharp periodic pattern,
not only a blurred one, and an audit that does not address that counter-case
has not made its argument however confident the prose.

## 19:55Z — stage 83 verifier FAIL, and the card was the cause

`Claude Opus 5` returned FAIL on 6 criteria: five PASS, criterion 4 failed.

The audit asserted a nine-write-up census, dropped six without mentioning
them, and closed by enumerating which promotion decisions need reopening —
presenting the census as complete. `docs/sweeps/` holds **fifteen**
`*-interestingness.md` files. Two concrete gaps:

- `clifford-dejong-interestingness.md` scores on the same metric stack (:14-15)
  and promotes three shipped presets on it (:38-44) — Clifford veils 0.481 →
  0.740, De Jong web→swan 0.435 → 0.762, De Jong scroll→heart 0.262 → 0.768.
  Absent from the table and not declared as "none".
- `gray-scott-interestingness.md:73-75` records that Spots and Waves use
  *promoted* parameters whose pre-promotion values "no longer exist" — another
  promotion pair, neither tabulated nor declared absent.

**The card caused this.** It said "nine write-ups" in five places, because the
orchestrator authored it from a truncated `ls | tail -8` and inferred nine from
eight visible files plus BZ. The worker's census matched the scope it was
given.

That does not make the worker blameless and the re-brief says so: it asserted a
specific nine-file list with no basis given, and the directory was one `ls`
away. The card's own Escalation clause — "An incomplete table presented as
complete is worse than no table" — describes exactly this case, and the right
move was to stop on the mismatch rather than proceed.

Diagnosis: **card defect**, compounded by a worker taking a stated scope on
trust. Not a work defect in the analysis itself — criteria 3, 5 and 6 all
passed, and the verifier called the phase-scrambled surrogate design at :96-135
specific enough to write a later card from.

Remedy: the card body corrected to fifteen in all five places; re-brief
appended naming both halves of the fault and the two gaps to close; attempt 1's
154-line audit preserved at `df560c52` and cited, so attempt 2 extends it
rather than restarting. Requeued.

## 19:43-20:03Z — twenty minutes of nothing, two causes stacked

The queue sat at five pending with no agent running. Neither cause was visible
from stage state; both were in the tick log.

**Cause 1: a stale vendor stamp.** From 19:43Z the tick logged, once per tick
and dispatching nothing:

```
stale vendor: emergence-lab holds the contract from fb08d95,
autometta is at 9d88a37
```

`autometta refresh-repo` reported **0 new, 0 updated, 6 unchanged** — every
vendored file was already current and only the stamp had fallen behind, because
autometta advanced past this repo's last vendor point when its cards 129/130
and the batch handoff landed. Twenty minutes of dispatch lost to a version
marker. Restamped and committed.

**Cause 2, revealed by fixing cause 1: the reserve config had become a dispatch
window.** The tick then logged:

```
schedule stop worker 82 (claude): clock 21:01 is outside the 01:00-06:00
dispatch window; it next opens at 01:00; no new dispatch this tick
```

`window_reserve.overnight.start`/`end` does double duty in
`quota_schedule_permits_dispatch`: it sets the reserve percentage *and*
declares a hard window outside which no new worker dispatch happens at all.
Arming the operator's "80% cap from 01:00" as an overnight block had therefore
silently confined the entire run to 01:00-06:00 — the exact opposite of the
instruction, which was to leave 22:00-01:00 uncapped.

There is no way to express "uncapped tonight, capped from 01:00" in that block.
Remedy: the overnight block is deleted, leaving top-level `percent: 0, action:
hold` so tonight runs uncapped; and a one-shot LaunchAgent
(`com.autometta.arm-reserve-0100`) sets the top-level `percent: 20` at 01:00,
which arms the reserve without declaring a window. Verified after the change
that dispatch resolves as permitted at 21:05, 23:30 and 02:30.

Worth a card in autometta: one key controlling two unrelated behaviours, where
setting it for the second silently disables the first.

## 20:03Z — dispatch resumed

Stage 82 worker (Claude Opus 5, pid 78352) running, attempt 2. Quota at the
restart: claude 5-hour 86.0% used resetting 20:59:59Z, weekly 9.0%; codex
5-hour 65.0% resetting 21:05:54Z, weekly 10.0%.
