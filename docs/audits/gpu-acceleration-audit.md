# GPU Acceleration Audit

## Scope

This audit covers the 12 simulation kernels listed in `HANDOFF.md:17-22`. The current WebGL2 path is a renderer backend, not a compute backend: `WebGLRendererBackend.draw()` receives a `Float32Array` state from the kernel, configures a texture, uploads that state, and draws a fragment shader (`src/app/webglRenderer.ts:336-358`, `src/app/webglRenderer.ts:484-506`). The Canvas path similarly maps the kernel state on the CPU (`src/app/canvasRenderer.ts:42-72`). Current backend below therefore means the simulation compute backend.

The relevant interface constraint is that `step(dt)` is synchronous, workers/WASM/GPU are internal kernel details, and `readState()` returns a stable pre-allocated `Float32Array` (`docs/INTERFACE.md:61-90`). That contract can tolerate internal GPU compute only if the kernel can still expose a current CPU-readable `Float32Array` without making `step()` async. A true GPU-resident state path would create interface tension because the renderer currently consumes CPU state after every `step()`.

## Kernel Classification

| Sim | Current backend | Per-step compute profile | GPU fit | Estimated speedup | Worker effort | Contract impact |
|---|---|---|---|---|---|---|
| Gray-Scott | CPU JS TypeScript kernel, with `step()` running nested grid loops over `stepsPerFrame` and `readState()` returning CPU state (`src/sims/gray-scott/kernel.ts:64`, `src/sims/gray-scott/kernel.ts:160`, `src/sims/gray-scott/kernel.ts:174-209`, `src/sims/gray-scott/kernel.ts:212-214`). | PDE stencil: 2-channel reaction-diffusion, 4-neighbour Laplacian, default 12 passes per frame (`src/sims/gray-scott/kernel.ts:110-117`, `src/sims/gray-scott/kernel.ts:191-204`). Rough default cost: about `12 * width * height` cell updates per app step, each touching neighbour U/V values. | High | 10x-50x if compute stays GPU-resident; 3x-15x if every frame still reads back to CPU. | M | The current `Float32Array` readback contract works for a WebGL2 ping-pong prototype, but readback each frame is the main tension with `docs/INTERFACE.md:77-90`. No interface bump if the kernel mirrors/readbacks internally. |
| Abelian Sandpile | CPU JS TypeScript kernel using a queue plus `Float32Array` state (`src/sims/abelian-sandpile/kernel.ts:49`, `src/sims/abelian-sandpile/kernel.ts:93-100`, `src/sims/abelian-sandpile/kernel.ts:153-201`, `src/sims/abelian-sandpile/kernel.ts:203-205`). | Event/queue toppling: adds center grains, then processes up to default 50,000 queued topples per step (`src/sims/abelian-sandpile/kernel.ts:26-30`, `src/sims/abelian-sandpile/kernel.ts:164-195`). Rough default cost: up to 50k queue iterations plus neighbour enqueue writes. | Medium | 3x-20x for a dense relaxation shader; lower while preserving sparse queue semantics. | L | The synchronous `step()` contract is tolerable, but exact queue ordering is CPU-shaped. A GPU relaxation version could keep `readState()` as `Float32Array`, but a fully GPU-resident version would stress `docs/INTERFACE.md:87-90`. |
| Game of Life | CPU JS TypeScript kernel, double-buffered with `Float32Array` state and `next` arrays (`src/sims/game-of-life/kernel.ts:66`, `src/sims/game-of-life/kernel.ts:119-127`, `src/sims/game-of-life/kernel.ts:178-221`, `src/sims/game-of-life/kernel.ts:223-225`). | Boolean cellular update: every cell reads 8 neighbours, applies B/S ranges, then copies next state (`src/sims/game-of-life/kernel.ts:192-220`). Rough default cost: `width * height` neighbour-count updates per app step. | High | 10x-60x if kept on GPU; 4x-20x with CPU readback. | S | Fits the current shape cleanly: ping-pong texture compute can still return a CPU `Float32Array` snapshot via internal readback, matching `docs/INTERFACE.md:77-90`. |
| Belousov-Zhabotinsky | CPU JS TypeScript kernel, 3-channel `Float32Array` state and nested grid stencil loops (`src/sims/belousov-zhabotinsky/kernel.ts:66`, `src/sims/belousov-zhabotinsky/kernel.ts:132-141`, `src/sims/belousov-zhabotinsky/kernel.ts:195-203`, `src/sims/belousov-zhabotinsky/kernel.ts:260-317`). | PDE stencil: 3-channel reaction-diffusion, 4-neighbour Laplacian, default 2 internal passes per app step (`src/sims/belousov-zhabotinsky/kernel.ts:26-33`, `src/sims/belousov-zhabotinsky/kernel.ts:288-312`). Rough default cost: about `2 * width * height` 3-channel stencil updates. | High | 8x-40x if GPU-resident; 3x-12x with readback. | M | Similar to Gray-Scott: the sync API can hide WebGL2 ping-pong internally, but per-frame readback is the bottleneck under `docs/INTERFACE.md:87-90`. |
| Boids | CPU JS TypeScript kernel with particle arrays and a rasterised 4-channel `Float32Array` output (`src/sims/boids/kernel.ts:105`, `src/sims/boids/kernel.ts:190-205`, `src/sims/boids/kernel.ts:265-343`, `src/sims/boids/kernel.ts:385-413`). | Particle update: all-pairs neighbour scan over default 80 boids, velocity integration, then rasterisation to grid (`src/sims/boids/kernel.ts:26-34`, `src/sims/boids/kernel.ts:276-334`, `src/sims/boids/kernel.ts:392-410`). Rough default cost: about 6,400 pair checks plus raster pass. | Low | 1x-4x at current default; 10x+ only after spatial hashing or much higher boid counts. | XL | GPU compute is possible, but not a good first target. Maintaining CPU `Float32Array` raster output fits `docs/INTERFACE.md:77-90`; a useful GPU particle path would probably want a separate particle-state renderer later. |
| Lorenz Attractor | CPU JS TypeScript kernel integrating one trajectory and depositing into a `Float32Array` density field (`src/sims/lorenz-attractor/kernel.ts:72`, `src/sims/lorenz-attractor/kernel.ts:125-137`, `src/sims/lorenz-attractor/kernel.ts:176-191`, `src/sims/lorenz-attractor/kernel.ts:193-195`). | Serial ODE integration: fades the full field, then performs default 24 RK4 integration/deposit steps (`src/sims/lorenz-attractor/kernel.ts:26-35`, `src/sims/lorenz-attractor/kernel.ts:181-190`, `src/sims/lorenz-attractor/kernel.ts:213-237`, `src/sims/lorenz-attractor/kernel.ts:250-309`). Rough default cost: one full-field fade plus 24 serial RK4/deposit passes. | Low | 1x-5x; only the fade/deposit field operations parallelise cleanly. | L | The main trajectory is serial, so GPU acceleration would add complexity without much interface benefit. Existing sync `step()` and `Float32Array` output are adequate. |
| Diffusion-Limited Aggregation | CPU JS TypeScript kernel launching walkers against a CPU `Float32Array` cluster (`src/sims/diffusion-limited-aggregation/kernel.ts:71`, `src/sims/diffusion-limited-aggregation/kernel.ts:124-133`, `src/sims/diffusion-limited-aggregation/kernel.ts:181-190`, `src/sims/diffusion-limited-aggregation/kernel.ts:253-279`). | Stochastic random walks: default 64 walkers, each up to 256 walk steps, with neighbour checks and possible cluster writes (`src/sims/diffusion-limited-aggregation/kernel.ts:26-31`, `src/sims/diffusion-limited-aggregation/kernel.ts:186-189`, `src/sims/diffusion-limited-aggregation/kernel.ts:256-264`, `src/sims/diffusion-limited-aggregation/kernel.ts:344-350`). Rough default cost: worst-case about 16k walker iterations per app step. | Medium | 4x-25x if many walkers run in parallel; less if atomic cluster growth dominates. | XL | GPU walkers need scatter/atomic-style writes or multi-pass conflict resolution. The current `Float32Array` output contract can be preserved, but exact deterministic growth semantics may be hard under `docs/INTERFACE.md:151-151`. |
| Elementary Cellular Automata | CPU JS TypeScript kernel with row buffers and a scrolling CPU history field (`src/sims/elementary-cellular-automata/kernel.ts:59`, `src/sims/elementary-cellular-automata/kernel.ts:94-101`, `src/sims/elementary-cellular-automata/kernel.ts:138-152`, `src/sims/elementary-cellular-automata/kernel.ts:185-214`). | 1D cellular update plus history scroll: default 1 generated row per app step, then `copyWithin` shifts the whole display history (`src/sims/elementary-cellular-automata/kernel.ts:26-29`, `src/sims/elementary-cellular-automata/kernel.ts:143-150`, `src/sims/elementary-cellular-automata/kernel.ts:191-207`). Rough default cost: `width` rule evaluations plus a `width * height` memory shift. | High | 3x-15x, mostly by moving history scroll and row write onto GPU. | M | Fits WebGL2 ping-pong textures, though the vertical-scroll history representation makes it slightly less direct than full-grid automata. CPU `Float32Array` readback remains the contract cost. |
| Brian's Brain | CPU JS TypeScript kernel, double-buffered `Float32Array` cellular automaton (`src/sims/brians-brain/kernel.ts:64`, `src/sims/brians-brain/kernel.ts:99-105`, `src/sims/brians-brain/kernel.ts:144-191`, `src/sims/brians-brain/kernel.ts:193-195`). | Boolean/tristate cellular update: every cell reads 8 neighbours and transitions alive to dying to dead (`src/sims/brians-brain/kernel.ts:156-190`). Rough default cost: `width * height` neighbour-count updates per app step. | High | 10x-60x if GPU-resident; 4x-20x with CPU readback. | S | Very compatible with WebGL2 ping-pong textures and the current sync kernel API. Internal readback can satisfy `docs/INTERFACE.md:77-90`. |
| Mandelbrot | CPU JS TypeScript kernel precomputing an escape field on init, then cycling palette phase in `step()` (`src/sims/mandelbrot/kernel.ts:59`, `src/sims/mandelbrot/kernel.ts:121-126`, `src/sims/mandelbrot/kernel.ts:128-179`, `src/sims/mandelbrot/kernel.ts:194-235`). | Escape iteration on init/parameter change, not per-frame compute; default max 128 iterations per pixel. Per app step only phase-adds across the field (`src/sims/mandelbrot/kernel.ts:26-31`, `src/sims/mandelbrot/kernel.ts:172-179`, `src/sims/mandelbrot/kernel.ts:217-232`, `src/sims/mandelbrot/kernel.ts:237-241`). Rough default cost: init is up to `width * height * 128`; step is `width * height`. | High | 20x-100x for recompute on pan/zoom; 3x-10x for current palette-step path. | M | GPU escape compute fits fragment shaders well. A CPU `Float32Array` result can still be read back, but best UX would avoid readback by rendering escape values directly, which conflicts with renderer reliance on `readState()`. |
| Julia Set | CPU JS TypeScript kernel precomputing base escape values, then phase-shifting state in `step()` (`src/sims/julia-set/kernel.ts:76`, `src/sims/julia-set/kernel.ts:156-167`, `src/sims/julia-set/kernel.ts:169-220`, `src/sims/julia-set/kernel.ts:233-295`). | Escape iteration on init/parameter change, default max 128 iterations per pixel; per app step phase-adds across state (`src/sims/julia-set/kernel.ts:26-34`, `src/sims/julia-set/kernel.ts:213-220`, `src/sims/julia-set/kernel.ts:248-274`, `src/sims/julia-set/kernel.ts:289-294`). Rough default cost: init is up to `width * height * 128`; step is `width * height`. | High | 20x-100x for recompute on parameter changes; 3x-10x for current palette-step path. | M | Same contract profile as Mandelbrot: WebGL2 fragment compute is natural, but true no-readback rendering would need an interface/renderer path beyond `Float32Array`. |
| Burning Ship | CPU JS TypeScript kernel precomputing escape values, then phase-shifting the CPU state (`src/sims/burning-ship/kernel.ts:56`, `src/sims/burning-ship/kernel.ts:118-123`, `src/sims/burning-ship/kernel.ts:125-159`, `src/sims/burning-ship/kernel.ts:183-256`). | Escape iteration on init/parameter change, default max 128 iterations per pixel, with abs-valued recurrence; per app step phase-adds across state (`src/sims/burning-ship/kernel.ts:26-33`, `src/sims/burning-ship/kernel.ts:152-159`, `src/sims/burning-ship/kernel.ts:198-230`, `src/sims/burning-ship/kernel.ts:252-255`). Rough default cost: init is up to `width * height * 128`; step is `width * height`. | High | 20x-100x for recompute on pan/zoom; 3x-10x for current palette-step path. | M | Fragment-shader escape compute is a strong fit. Keeping the current contract requires readback; a direct shader-render path would create interface tension with `docs/INTERFACE.md:77-90`. |

## Priority Targets

1. **Brian's Brain - WebGL2 ping-pong textures.** It is the smallest high-payoff target: one channel, discrete states, two buffers, local 8-neighbour reads, and no multi-channel coupling. Follow-on card deliverable: add an internal GPU-backed Brian's Brain kernel or GPU compute helper behind the existing `SimKernel` methods, keep deterministic CPU fallback, and verify parity on small grids.

2. **Game of Life - WebGL2 ping-pong textures.** Same simple cellular shape as Brian's Brain, with standard neighbour counting and single-channel output. Follow-on card deliverable: implement GPU next-state pass, preserve the CPU kernel for fallback/tests, and add a comparison test over a small seeded grid.

3. **Gray-Scott - WebGL2 ping-pong textures first, WebGPU later.** This has the biggest visible payoff because the default performs 12 full-grid stencil passes per app step. Follow-on card deliverable: prototype a two-channel floating-point ping-pong compute path, measure whether CPU readback erases the gain, and document any need for a future renderer contract that can consume GPU-resident state.

Fractals are also strong GPU fits, but the current kernels do their expensive escape iteration on init or parameter changes and only phase-shift during `step()`. They are good second-wave targets once there is a clearer answer on whether the renderer can bypass CPU `Float32Array` state for direct shader rendering.
