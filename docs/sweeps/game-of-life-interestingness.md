# Game of Life interestingness sweep — 2026-08-23

Generated by the Playwright interestingness-sweep harness (`e2e/`). Run it with:

```bash
SWEEP=1 npx playwright test sweep.spec.ts -g "sweep game of life"
```

Artifacts land under `e2e/artifacts/game-of-life/` (git-ignored). The config is
`GAME_OF_LIFE` in `e2e/harness/sims.ts`. Metric stack unchanged — see
`docs/sweeps/gray-scott-interestingness.md`.

## Harness settings

128×128 grid, channel 0 (Alive), 200 warmup steps — long enough for a random
soup to burn down to its ash of still lifes, blinkers and gliders — flux gap 4,
coverage threshold 0.1.

## Coverage of the search

- **Sets evaluated: 45** — surviveMin ∈ {1, 2, 3} × surviveMax ∈ {3, 4, 5} ×
  seedDensity ∈ {0.05, 0.12, 0.2, 0.28, 0.4}.
- **References evaluated: 3** — the shipped presets, unmodified.
- **Sets skipped: 315.** The unbudgeted grid is 6 × 6 × 10 over the same ranges.
- **Not searched at all: 4 params** — `birthMin`/`birthMax` (both pinned at 3;
  moving them leaves the Life-like family altogether and all three shipped
  presets are B3), `sparkRate`, `ageShading`.

## Ranking

| | set | score | entropy | autocorr | flux | coverage |
|---|---|---|---|---|---|---|
| 1 | S2–3, seed 0.4 | **0.411** | 0.30 | 0.50 | 0.0595 | 0.190 |
| 2 | S3–5, seed 0.05 | 0.387 | 0.55 | 0.25 | 0.0535 | 0.420 |
| 3 | S2–3, seed 0.28 | 0.374 | 0.24 | 0.49 | 0.0412 | 0.138 |

| reference (as shipped before this stage) | score | entropy | autocorr | flux | coverage |
|---|---|---|---|---|---|---|
| Conway — S2–3, seed 0.28 | 0.374 | 0.24 | 0.49 | 0.0412 | 0.138 |
| Dense ash — S2–3, seed 0.28 | 0.374 | 0.24 | 0.49 | 0.0412 | 0.138 |
| Maze-like — S1–5, seed 0.05 | 0.082 | 0.21 | **−0.07** | 0.0006 | 0.539 |

## Finding 1: "Dense ash" was a byte-for-byte duplicate of "Conway"

Both presets shipped `{ birthMin: 3, birthMax: 3, surviveMin: 2, surviveMax: 3,
seedDensity: 0.28 }`. They scored identically to every decimal place because they
were the same parameter set — two gallery entries producing one simulation.

## Promotion: Dense ash, 0.374 → 0.411

```
seedDensity: 0.28  →  0.4
```

`S2–3, seed 0.4` was the top-scoring set of the whole sweep, and it is the
densest B3/S23 regime available: coverage 0.138 → 0.190, entropy 0.24 → 0.30,
flux 0.0412 → 0.0595. Modest as a composite delta (+9.9%), but the real win is
that the preset now does what its label says and is distinct from Conway.
Verified: re-running with the promoted params reproduces 0.411 exactly.

Conway itself is left alone — it is the canonical rule at a canonical density,
the same reasoning that kept Lorenz at ρ=28 and Clifford at its classic
coefficients.

## Finding 2: "Maze-like" scores 0.082, and the metric is wrong about it

B3/S12345 at seed 0.05 stabilises into a static maze: coverage 0.539, temporal
flux 0.0006, spatial autocorrelation **−0.07**. The negative autocorrelation is
the whole story — a maze of one-cell-wide corridors alternating with one-cell
walls is anti-correlated at lag 1, which is exactly the high-frequency signal the
structure term is built to punish (it is the same term that starves white noise).

That is a limitation of a lag-1 coherence measure, not a defect in the preset.
The maze looks like a maze; it simply lives at a spatial frequency the metric
cannot see as structure. **Not promoted, and not escalated as a defect** — it is
recorded here so a future sweep does not "fix" a working preset on this number.

A multi-lag or FFT-band structure term would rank fine-scale periodic patterns
correctly. Out of scope for this stage; noted as follow-up.
